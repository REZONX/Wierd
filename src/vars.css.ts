import { createGlobalTheme } from "@vanilla-extract/css";

export const vars = createGlobalTheme(":root",{
    sectionTitle:{
        hotLine:"Rencently Top Movies",
        comingSoonToTheaters:'Trailers for upcoming releases',
    }
})