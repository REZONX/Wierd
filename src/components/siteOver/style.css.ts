import { style } from "@vanilla-extract/css";

export const container = style({
    position: "relative",
    // marginBottom:"47.9rem",
    zIndex:"5",
    boxShadow:"0 0 20px rgb(0 0 0 / 25%)",
    paddingBottom:"68rem",
})

export const inside = style({
    background: "#eee"
})